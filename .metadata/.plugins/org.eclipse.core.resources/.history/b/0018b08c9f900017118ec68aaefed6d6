# Spring properties
spring:
  application:
     name: user-service

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:11000/eureka/

# HTTP Server
server:
  port: 11001   # HTTP (Tomcat) port