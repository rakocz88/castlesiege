
# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:11000/eureka/

# HTTP Server
server:
  port: 14001   # HTTP (Tomcat) port
  
spring:
  application:
    name: e2e-tests
logging:
  level:
    com.pilaf.cs.tests.feign.UserFeignClient: DEBUG

